kind = "web"
previewPath = "/"
title = "ReplitReasoner"
version = "1.0.0"
id = "artifacts/reasoner"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 20899

[services.development]
run = "pnpm --filter @workspace/reasoner run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/reasoner", "run", "build" ]
publicDir = "artifacts/reasoner/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "20899"
BASE_PATH = "/"
